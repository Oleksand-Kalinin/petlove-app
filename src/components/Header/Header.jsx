import Container from "../Container/Container.jsx";
import Logo from "../Logo/Logo.jsx";

const Header = () => {
  return (
    <header>
      <Container>
        <Logo />
      </Container>
    </header>
  );
};

export default Header;
